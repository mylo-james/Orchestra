# Story 1.7: Context-aware Loader API (load_persona(pid, team_id, project_id))

## Status
Spec In Progress

## Story
**As a** Scrum Master,
**I want** a context-aware loader API to load personas by team and project,
**so that** the correct overlay stack is applied at runtime.

## Acceptance Criteria
1. API: `load_persona(pid, team_id, project_id)` implemented
2. Deterministic overlay application and verification
3. Logging/tracing includes team/project/tags/policyVersion
4. Graceful failure and fallback behaviors defined
5. Unit and integration tests cover key scenarios

## Tasks / Subtasks
- [ ] Implement API surface and integration points (AC: 1)
- [ ] Apply overlay merge engine results (AC: 2)
- [ ] Structured tracing/logging with correlation IDs (AC: 3)
- [ ] Error handling with retries and partial rollback (AC: 4)
- [ ] Tests for success/failure/edge cases (AC: 5)

## Dev Notes
- Cross-cutting ACs from Epic 1 specify tracing and idempotency. See `docs/epic-1-bmad-foundation.md`.

### Testing
- Unit tests for API input validation and output correctness
- Integration tests with sample overlays and personas

## Spec Agent Record

### Agent Model Used
- Geoffrey (@spec)

### Test Plan
- Unit: tests/unit/api/test_load_persona_api_signature_and_validation.py
  - Verifies API `load_persona(pid, team_id, project_id)` signature and validation (AC:1)
  - RED now: API not implemented
- Unit: tests/unit/api/test_overlay_application_is_deterministic.py
  - Ensures deterministic overlay application and verification (AC:2)
  - RED now: merge/integration missing
- Unit: tests/unit/api/test_logging_and_tracing_fields_present.py
  - Asserts logs/traces include team/project/tags/policyVersion (AC:3)
  - RED now: instrumentation missing
- Integration: tests/integration/api/test_failure_and_fallback_behaviors.py
  - Exercises graceful failure, retries, partial rollback (AC:4)
  - RED now: behaviors not implemented
- Integration: tests/integration/api/test_end_to_end_key_scenarios.py
  - Success/failure/edge cases coverage (AC:5)
  - RED now: loaders/overlays not integrated

### Test Coverage Log
- 2025-08-24: Spec created; tests expected RED pending API and overlay integration.

### Completion Notes List
- Pending

## Change Log
| Date | Version | Description | Author |
| ---- | ------- | ----------- | ------ |
| 2025-08-24 | 0.1 | Initial draft | SM |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
### Gate Status
Gate: CONCERNS → qa.qaLocation/gates/1.7-context-aware-loader-api.yml
